// Reference passing in A7

io :: import "std/io"

increment :: fn(p: ref i32) {
    p += 1
}

main :: fn() {
    io.println("=== Pointers ===")

    x: i32
    x = 10

    io.println("before = {}", x)
    increment(x)
    io.println("after = {}", x)
}
