# API and SDK (alias)

Canonical page: [API and SDK](/a7-py/docs/guide/api.md).

The canonical API page lists current automation entry points (`uv run a7`, `uv run python main.py`, the `--format json` outputs, and `scripts/` automation hooks) and known limits.
