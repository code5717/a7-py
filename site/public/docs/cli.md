# CLI (alias)

Canonical page: [CLI](/a7-py/docs/guide/cli.md).

The canonical CLI reference covers entrypoints, modes, flags, exit codes, and structured JSON output. This alias exists for agents that guess `/docs/cli.md` directly.
