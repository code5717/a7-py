# Installation (alias)

Canonical page: [Installation](/a7-py/docs/install.md).

The canonical install page covers Python, uv, Zig 0.15.2, and the local docs build. This alias exists so agents guessing `/docs/installation.md` still land on the right content.
